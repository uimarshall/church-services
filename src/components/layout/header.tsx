export default function Header() {
  return (
    <header className="flex h-16 flex-shrink-0 items-center justify-between border-b bg-white px-4">
      {/* Add header content here, e.g., search bar, user menu */}
      <div />
      <div>User Menu</div>
    </header>
  );
}
